{% set name = "pyearth" %}
{% set version = "0.1.18" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyearth-{{ version }}.tar.gz
  sha256: 367866ee9ac90f65dd69bfd82a7f7b6303eeff68399271cf6558869f90d556e5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
  run:
    - cartopy
    - gdal
    - matplotlib-base
    - netcdf4
    - numpy
    - pandas
    - python >=3.8
    - scipy
    - statsmodels

test:
  imports:
    - pyearth
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/changliao1025/pyearth
  summary: Python for Earth Science.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - changliao1025
